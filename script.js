function checkPassword() { const errorMessage = document.getElementById('error'); fetch('pw.txt') .then(response => response.text()) .then(correctPassword => { const inputPassword = document.getElementById('password').value; if (inputPassword === correctPassword) { (async function preload() { try { const response = await fetch('bhunjimko.html'); const fileContent = await response.text(); const newWindow = window.open('about:blank', 'noopener, noreferrer'); newWindow.document.open(); newWindow.document.write(fileContent); newWindow.document.close(); } catch (error) { console.error('Error fetching or opening the file:', error); } })(); errorMessage.style.display = 'none'; } else { errorMessage.style.display = 'block'; } }) .catch(error => { console.error('Error reading password file:', error); errorMessage.style.display = 'block'; }); }
